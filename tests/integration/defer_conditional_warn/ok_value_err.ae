// Both forms are meaningful here: the function has an error channel.
// MUST compile with no warning.
f(fail: bool) -> (int, string) {
    defer catch println("rollback")
    defer try   println("commit")
    if fail { return 0, "boom" }
    return 1, ""
}
main() { _, _ = f(false) }
