// Same, through `T!`. MUST compile with no warning.
f(fail: bool) -> int! {
    defer catch println("rollback")
    defer try   println("commit")
    if fail { return 0, "boom" }
    return 1
}
main() { _, _ = f(false) }
