// Deliberately broken module for the #646 regression: a bare field
// declaration at top level is not a valid module item. The parse error
// originates HERE, so the diagnostic must name this file (module.ae)
// and render this line — not the importing main.ae.
// comment line to push the error onto a known line number
    module_orphan_field: long
}
