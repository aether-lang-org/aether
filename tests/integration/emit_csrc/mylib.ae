// Source for the --emit=csrc regression (aether#996).
exports (add, greet, VERSION)

const VERSION = "1.0"

add(a: int, b: int) -> int {
    return a + b
}

greet(name: string) -> string {
    return name
}
