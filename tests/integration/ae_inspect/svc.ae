// Fixture for `ae inspect` (issue #473): a small service-shaped script
// with gated imports, a struct, a const, a function, and an entry point.
import std.string
import std.fs

struct Config { int port  int workers }

const DEFAULT_PORT = 8080

greet(name: string) -> string {
    return "hello"
}

main() {
    println(greet("world"))
}
