// Control: a complete match must pass.
struct Circle { r: float }
struct Rect   { w: float  h: float }
type Shape = Circle | Rect
f(s: Shape) -> int {
    match s {
        Circle -> { return 1 }
        Rect   -> { return 2 }
    }
    return 0
}
main() { println("${f(Circle { r: 1.0 })}") }
